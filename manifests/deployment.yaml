apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: compute-resources-area-calidad-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:10.4.3
          ports:
            - containerPort: 3000
          env:
            - name: GF_SECURITY_ADMIN_USER
              value: admin
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: admin123
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: influxdb
  namespace: compute-resources-area-calidad-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: influxdb
  template:
    metadata:
      labels:
        app: influxdb
    spec:
      containers:
        - name: influxdb
          image: influxdb:3.9.0-core
          args:
            - influxdb3
            - serve
            - --node-id
            - influxdb
            - --object-store
            - file
            - --data-dir
            - /var/lib/influxdb3/data
            - --admin-token-file
            - /etc/influxdb3/admin-token/admin-token.json
          ports:
            - containerPort: 8181
          env:
            - name: INFLUXDB3_LOG_FILTER
              value: info
          volumeMounts:
            - name: influxdb-data
              mountPath: /var/lib/influxdb3/data
            - name: influxdb-admin-token
              mountPath: /etc/influxdb3/admin-token
              readOnly: true
      volumes:
        - name: influxdb-data
          emptyDir: {}
        - name: influxdb-admin-token
          secret:
            secretName: influxdb-admin-token
---
apiVersion: v1
kind: Secret
metadata:
  name: influxdb-admin-token
  namespace: compute-resources-area-calidad-dev
type: Opaque
stringData:
  admin-token.json: |
    {
      "token": "apiv3_change_this_token_before_prod",
      "name": "_admin",
      "description": "Admin token for InfluxDB 3 Core on Kubernetes"
    }
---
apiVersion: v1
kind: Secret
metadata:
  name: influxdb-explorer-config
  namespace: compute-resources-area-calidad-dev
type: Opaque
stringData:
  config.json: |
    {
      "DEFAULT_INFLUX_SERVER": "http://influxdb:8181",
      "DEFAULT_API_TOKEN": "apiv3_change_this_token_before_prod",
      "DEFAULT_SERVER_NAME": "InfluxDB Core 3.9"
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: influxdb-explorer
  namespace: compute-resources-area-calidad-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: influxdb-explorer
  template:
    metadata:
      labels:
        app: influxdb-explorer
    spec:
      containers:
        - name: influxdb-explorer
          image: influxdata/influxdb3-ui:1.6.2
          args:
            - --mode=admin
          ports:
            - containerPort: 80
          volumeMounts:
            - name: influxdb-explorer-config
              mountPath: /app-root/config
              readOnly: true
      volumes:
        - name: influxdb-explorer-config
          secret:
            secretName: influxdb-explorer-config
