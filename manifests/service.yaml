apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: compute-resources-area-calidad-dev
spec:
  selector:
    app: influxdb
  ports:
    - name: http
      port: 8181
      targetPort: 8181
  type: ClusterIP
